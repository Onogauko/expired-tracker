name: Daily WhatsApp Expiry Notification

on:
  workflow_dispatch:

  schedule:
    - cron: '0 22 * * *'

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Vercel Function
        run: curl https://expired-tracker.vercel.app/api/notify
